deploy_hello2:
    extends: .deploy_function_template
    stage: deploy
    environment:
        name: $CI_COMMIT_BRANCH/hello-2
    script:
        - gcloud functions deploy hello-2 --gen2 --entry-point=defaultHandler --runtime=nodejs20  --trigger-http --source=apps/functions/hello-2 --allow-unauthenticated
    rules:
        - if: '$CI_COMMIT_BRANCH == "staging" || $CI_COMMIT_BRANCH == "production"'
          changes:
              - apps/functions/hello-2/**
